# 快速开始

## 第一个工作流

### 启动智能分流工作流

在 Claude Code 中：

```
/gclm 添加用户认证功能
```

gclm-flow 会自动：
1. 列出可用工作流 (`workflow list`)
2. LLM 选择最匹配的工作流
3. 创建任务并开始第一阶段

**CODE_COMPLEX 工作流阶段**:
1. 读取项目文档 (Phase 0)
2. 分析需求并分类 (Phase 1)
3. 探索相关代码 (Phase 2)
4. 澄清疑问 (Phase 3)
5. 设计架构 (Phase 4)
6. 编写规范文档 (Phase 5)
7. 编写测试 (Phase 6)
8. 实现代码 (Phase 7)
9. 重构和审查 (Phase 8)
10. 完成总结 (Phase 9)

---

## 常用命令

### /gclm - 智能分流工作流

```
/gclm <任务描述>
```

**流程**:
1. 调用 `workflow list --json` 获取所有工作流
2. LLM 根据提示语义选择最匹配的工作流
3. 调用 `workflow start "<提示>" --workflow <name>`

**示例**:
```
/gclm 修复登录页面的样式问题
/gclm 编写 API 文档
/gclm 重构用户管理模块
```

---

### /investigate - 代码库调查

```
/investigate <问题>
```

**示例**:
```
/investigate 项目中如何处理用户认证？
/investigate 错误处理机制在哪里？
/investigate 数据库连接是怎么建立的？
```

---

### /tdd - 测试驱动开发

```
/tdd <功能>
```

**示例**:
```
/tdd 添加密码验证函数
/tdd 实现用户注册 API
```

---

### /spec - 规范驱动开发

```
/spec <功能>
```

**示例**:
```
/spec 设计用户权限系统
/spec 定义支付流程规范
```

---

### /llmdoc - 文档生成

```
/llmdoc
```

自动生成或更新项目文档。

---

## 工作流类型

### 📝 DOCUMENT 工作流

**触发关键词**: 文档、方案、设计、需求、分析

**示例**:
```
/gclm 编写用户认证方案文档
/gclm 设计系统架构图
/gclm 分析现有代码结构
```

**阶段**: Discovery → Exploration → Clarification → Draft → Refine → Review → Summary

---

### 🔧 CODE_SIMPLE 工作流

**触发关键词**: bug、修复、error、fix

**示例**:
```
/gclm 修复登录按钮不响应的问题
/gclm fix error in user service
/gclm 调试 API 返回错误
```

**阶段**: Discovery → Clarification → TDD Red → TDD Green → Refactor → Summary

---

### 🚀 CODE_COMPLEX 工作流

**触发关键词**: 功能、模块、开发、重构

**示例**:
```
/gclm 添加用户权限管理功能
/gclm 开发支付模块
/gclm 重构数据访问层
```

**阶段**: 全流程 9 阶段 (含 SpecDD)

---

## 快速参考

### Agent 速查

| Agent | 职责 | 使用阶段 |
|:---|:---|:---|
| `investigator` | 代码库调查 | Phase 1, 2, 9 |
| `architect` | 架构设计 | Phase 4 |
| `spec-guide` | 规范文档 | Phase 5 |
| `tdd-guide` | TDD 指导 | Phase 6 |
| `worker` | 任务执行 | Phase 7 |
| `code-reviewer` | 代码审查 | Phase 8 |

---

### TDD 循环

```
Red (写测试) → Green (写实现) → Refactor (重构)
```

**关键规则**:
- 绝不一次性生成代码和测试
- 先写测试，后写实现
- 测试必须先失败
- 覆盖率 > 80%

---

### 状态管理

**数据库存储**: 工作流状态保存在 SQLite 数据库 (`~/.gclm-flow/gclm-engine.db`)

**查看当前任务**:
```bash
# 列出所有任务
gclm-engine task list

# 查看任务详情
gclm-engine task get <task-id>

# 查看当前阶段
gclm-engine task current <task-id>

# 导出状态文件 (兼容旧版)
gclm-engine task export <task-id> .claude/gclm.local.md
```

**任务控制**:
```bash
# 暂停任务
gclm-engine task pause <task-id>

# 恢复任务
gclm-engine task resume <task-id>

# 取消任务
gclm-engine task cancel <task-id>
```

---

## 故障排查

### 问题: 工作流卡在某个阶段

**解决**:
```bash
# 查看任务状态
gclm-engine task get <task-id>

# 查看当前阶段
gclm-engine task current <task-id>

# 查看所有阶段
gclm-engine task phases <task-id>

# 查看事件日志
gclm-engine task events <task-id>
```

**如果需要强制跳过**:
```bash
# 标记阶段为完成
gclm-engine task complete <task-id> <phase-id> --output "手动跳过"
```

---

### 问题: auggie 搜索失败

**解决**:
1. 检查 auggie 是否安装: `which auggie`
2. 如未安装: `npm install -g @augmentcode/auggie@prerelease`
3. 系统会自动回退到 llmdoc + Grep

---

### 问题: llmdoc 不存在

**解决**: 调用 `/llmdoc` 自动生成，无需手动操作

### 问题: 工作流同步失败

**解决**:
```bash
# 验证工作流配置
gclm-engine workflow validate workflows/feat.yaml

# 强制同步
gclm-engine workflow sync --force
```

---

## 下一步

- 阅读 [架构文档](../architecture/system.md) 了解系统架构
- 阅读 [资源嵌入](../architecture/assets.md) 了解零依赖部署
- 阅读 [工作流配置](../architecture/workflows.md) 了解工作流定义
- 阅读 [Agent 文档](../architecture/agents.md) 了解各个 Agent
- 阅读 [命令参考](../reference/commands.md) 了解所有命令
